#include <stack>

void test1()

{

    stack<int> t;

    t.push(10);

    t.push(20);

    t.push(30);

    t.push(40);

    while(t.size() != 0)

    {

        cout << "栈顶为" << t.top() << endl;  // 4 3 2 1

        //弹出栈顶元素

        t.pop();

    }

    cout << "size : " << t.size() << endl;

}
