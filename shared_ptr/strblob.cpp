#include <iostream>
#include <vector>
#include <string>
#include <memory>

/*
using namespace std;

class StrBlob
{
    public:
        friend void show();
        StrBlob();

        void push(int s);
        void pop();
        bool checksize(string &msg);

    private:
        shared_ptr<vector<int>> vec;
};

void show()
{
    for(it1; it1 != vec.end(); ++it1)
    {
        cout << *it1 << endl;
    }
}

void StrBlob::push(int s)
{
    vec->push_back(s);
}

void StrBlob::pop()
{
    vec->pop_back();
}

bool StrBlob::checksize(const strng &msg)
{
    if(!vec->empty())
        return true;
    else
    {
        throw out_of_range(msg);
        return false;
    }
}

int main(int argc, char **argv)
{
    StrBlob vc;
    if(vc.checksize("NULL") 
    {
        vc.push();
        vc.pop();
        vc.show();
    }

    return 0;
}
*/

